const body=document.body;const overlay=document.getElementById('finishOverlay');const chips=[...document.querySelectorAll('.chip')];const views=[...document.querySelectorAll('.seg')];function setFinish(btn){chips.forEach(c=>c.classList.toggle('active',c===btn));const src=btn.dataset.src||'';overlay.classList.remove('visible');if(!src){overlay.removeAttribute('src');return}const img=new Image();img.onload=()=>{overlay.src=src;requestAnimationFrame(()=>overlay.classList.add('visible'))};img.src=src}function setView(view){body.dataset.view=view;views.forEach(b=>b.classList.toggle('active',b.dataset.view===view))}chips.forEach(b=>b.addEventListener('click',()=>setFinish(b)));views.forEach(b=>b.addEventListener('click',()=>setView(b.dataset.view)));setFinish(document.querySelector('.chip.active'));
